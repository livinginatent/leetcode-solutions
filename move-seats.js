var minMovesToSeat = function (seats, students) {
  let count = 0;
  for (let i = 0; i < seats.length; i++) {
    if (!seats.includes(students[i])) {
      let closest = 
    }
  }
  return count;
};

//seats = [4,1,5,9], students = [1,3,2,6]

minMovesToSeat([4, 1, 5, 9], [1, 3, 2, 6]);
