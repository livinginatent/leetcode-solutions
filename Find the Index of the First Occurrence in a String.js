const strStr = (haystack, needle) => {
 /*  if (!haystack.includes(needle)) return -1;
  const arr = haystack.split(needle);
  const firstIndex = (element) => element === "";
  return arr.findIndex(firstIndex);
  */
 console.log(haystack.indexOf(needle))
};

strStr("hello", "ll");
