var minimizedStringLength = function (s) {
let res = ""
  for (let i = 1; i < s.length; i++) {
    let left = i-1
    let right = i+1
    if(s[left]===s[i]){
        
    }
  }
};
//s = "aaabc"

minimizedStringLength("aaabc");
